# dsh bundle layer: mount the free vision plugin (dsh/index.js via the
# package root export). The API key is read from the plugin config
# (apiKey) or from the matching provider environment variable.
- insert:
    - id: free-vision
      name: 'dsh-free-vision'
