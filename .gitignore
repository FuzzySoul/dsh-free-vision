# ── dependencies / build artifacts / local secrets ──────
node_modules/
*.log
.DS_Store
*.tgz
.npmrc

# local settings (contains API keys - never commit)
free-vision.json

# test/build artifacts
coverage/
dist/
out/
